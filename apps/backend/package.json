{
   "name": "backend",
   "version": "1.0.0",
   "description": "Python backend for the Electron React Python Template",
   "main": "main.py",
   "scripts": {
      "dev": "cross-env uv run uvicorn src.main:app --host 127.0.0.1 --reload",
      "build": "uv build",
      "clean": "rimraf build dist __pycache__ .pyinstaller *.spec"
   },
   "keywords": [],
   "author": "",
   "license": "ISC",
   "devDependencies": {
      "cross-env": "^7.0.3",
      "rimraf": "^5.0.1"
   }
}
