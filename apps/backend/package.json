{
   "name": "backend",
   "version": "1.0.0",
   "description": "Python backend for the Electron React Python Template",
   "main": "main.py",
   "scripts": {
      "dev": "./venv/bin/uvicorn main:app --reload",
      "build": "npm run clean && ./venv/bin/pyinstaller main.py --onedir --name backend_main --distpath dist --noconfirm --clean --strip",
      "clean": "rm -rf build dist __pycache__ .pyinstaller *.spec"
   },
   "keywords": [],
   "author": "",
   "license": "ISC"
}
