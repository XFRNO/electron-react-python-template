{
   "name": "electron-react-python-template",
   "private": true,
   "version": "1.0.0",
   "scripts": {
      "build": "turbo run build",
      "dev": "pnpm --filter electron dev",
      "postinstall": "pnpm --filter backend exec uv sync",
      "lint": "turbo run lint",
      "format": "prettier --write \"**/*.{ts,tsx,md}\"",
      "check-types": "turbo run check-types",
      "clean": "echo 🧹 Cleaning workspace... && rm -rf node_modules apps/*/node_modules packages/*/node_modules && rm -f package-lock.json bun.lockb bun.lock yarn.lock pnpm-lock.yaml && rm -f apps/*/package-lock.json apps/*/bun.lockb apps/*/bun.lock packages/*/package-lock.json packages/*/bun.lockb packages/*/bun.lock && rm -rf .turbo && echo 🧽 Clearing pnpm cache... && pnpm store prune && echo 🧼 Removing Python virtual environments... && rm -rf apps/*/.venv packages/*/.venv backend/.venv && echo ✅ Clean complete!"
   },
   "devDependencies": {
      "@types/node": "^24.10.0",
      "@types/wait-on": "^5.3.4",
      "prettier": "^3.6.2",
      "turbo": "^2.5.8",
      "typescript": "5.9.2"
   },
   "engines": {
      "node": ">=18"
   },
   "packageManager": "pnpm@10.19.0",
   "dependencies": {
      "dotenv": "^17.2.3",
      "get-port": "^7.1.0"
   }
}
