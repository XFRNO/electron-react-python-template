{
   "name": "electron-react-python-template",
   "private": true,
   "scripts": {
      "build": "turbo run build",
      "dev": "turbo run dev --parallel",
      "lint": "turbo run lint",
      "format": "prettier --write \"**/*.{ts,tsx,md}\"",
      "check-types": "turbo run check-types",
      "clean": "echo 🧹 Cleaning workspace... && rm -rf node_modules apps/*/node_modules packages/*/node_modules && rm -f package-lock.json bun.lockb bun.lock yarn.lock pnpm-lock.yaml && rm -f apps/*/package-lock.json apps/*/bun.lockb apps/*/bun.lock && rm -f packages/*/package-lock.json packages/*/bun.lockb packages/*/bun.lock && rm -rf .turbo && echo 🧽 Clearing npm and bun caches... && npm cache clean --force || true && bun pm cache clean || true && echo ✅ Clean complete!"
   },
   "devDependencies": {
      "prettier": "^3.6.2",
      "turbo": "^2.5.8",
      "typescript": "5.9.2"
   },
   "engines": {
      "node": ">=18"
   },
   "packageManager": "bun@1.3.1",
   "workspaces": [
      "apps/frontend",
      "apps/backend",
      "apps/electron",
      "packages/*"
   ]
}
